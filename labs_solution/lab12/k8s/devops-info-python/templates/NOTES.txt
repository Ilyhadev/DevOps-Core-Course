1. Release {{ .Release.Name }} was installed in namespace {{ .Release.Namespace }}.

2. Check workload status:
   kubectl get deploy,po,svc -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if eq .Values.service.type "NodePort" }}
3. Access application with NodePort:
   kubectl get svc {{ include "devops-info-python.fullname" . }} -o wide
   kubectl port-forward svc/{{ include "devops-info-python.fullname" . }} 18080:{{ .Values.service.port }}
   curl http://127.0.0.1:18080/health
{{- else if eq .Values.service.type "LoadBalancer" }}
3. Wait for external IP:
   kubectl get svc {{ include "devops-info-python.fullname" . }} -w
{{- else }}
3. Port-forward service locally:
   kubectl port-forward svc/{{ include "devops-info-python.fullname" . }} 18080:{{ .Values.service.port }}
{{- end }}
